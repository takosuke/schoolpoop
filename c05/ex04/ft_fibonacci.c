#include <stdio.h>

int ft_fibonacci(int index)
{
	int start;
	
	start = 0;
	if (index == 0) {
		return (0);
	}
	return 
}

int main(void)
{
	printf("fibonacci number %d: %d", 5, ft_fibonacci(5));
	return (0);
}
