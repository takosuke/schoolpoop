#include <stdio.h>
#include <stdlib.h>

void  ft_putstr_non_printable.c(char *str)
{
  while (*ptr != '\0')
  {
    if (*ptr < 32)
    {
      *ptr = 
        //TODO IM FALLING ASLEEP
        //the logic of converting non printable chars to alphanum
    }
  }
}

int main(0)
{
  char str[] = "There's some \n stuff here that shouldnt be here"
  ft_putstr_non_printable(str);
  return (0);
}
